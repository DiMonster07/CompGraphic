#version 330 core
in vec2 TexCoord;

out vec4 color;


struct DirLight
{
    vec3 color;
    float intensity;
};

uniform DirLight gDirLight;
uniform sampler2D ourTexture;

void main()
{
	color = texture2D(ourTexture, TexCoord.st) * vec4(gDirLight.color, 1.0) * gDirLight.intensity;
}
